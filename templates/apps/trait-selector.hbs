<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "sds.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "SdS.TraitSelectorSpecial"}}</label>
            <input type="text" name="{{customPath}}" value="{{custom}}">
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "SdS.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "SdS.DamagePhysicalBypassHint"}}</p>
        {{> "sds.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "SdS.TraitSave"}}</button>
</form>
